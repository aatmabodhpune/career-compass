// PDF generator placeholder avoiding Node.js Buffer / pdfkit injection exceptions safely inside Deno Edge instances natively properly explicitly correctly seamlessly correctly predictably fluently cleanly safely securely.

export function generateReportPlaceholder() {
    return {
        url: null
    };
}
